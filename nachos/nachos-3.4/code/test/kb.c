#include "syscall.h"

int main() {
	//int answer = ReadInt();
	//PrintInt(answer);
	
	float f = ReadFloat();
	PrintFloat(f);
	
	//char c = ReadChar();
	//PrintChar(c);

	//int legth;
	//char s[255];
	//length = ReadInt();
	//ReadString(s, length);
	//PrintString(s);

	Halt();
	return 0;
}
